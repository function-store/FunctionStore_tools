{
  "version": 1,
  "hidden": [
    "python:.embody/envoy-bridge.py",
    "python:.embody/tdn_textconv.py"
  ],
  "preferredPort": null,
  "env": {},
  "python": null,
  "node": null,
  "ignore": [],
  "runGroups": [],
  "comments": {
    "python:packaging/upload.py": "The ONLY shell-side script in this repo's release flow (step 4 of packaging/README). Syncs packaging/publish/ to the R2 bucket via wrangler, setting Cache-Control per file: release-pinned objects immutable, the rolling manifest.json no-cache. Already-uploaded pinned objects are skipped, so re-running after a flaky connection touches only what failed. Needs wrangler installed and authenticated, and a staged packaging/publish/ (Stage() runs inside TD). Run the --dry variant first. Everything else in packaging/ -- build_manifest, publish, release_one, build_installer, install -- runs INSIDE TouchDesigner and is not launchable from here.",
    "python:.embody/envoy-bridge.py": "Not a dev script. Embody's STDIO<->HTTP bridge to the Envoy MCP server inside TD; regenerated on every Envoy start and launched by the MCP client. Hidden because running it by hand does nothing useful.",
    "python:.embody/tdn_textconv.py": "Not a dev script. The git textconv driver that strips volatile header churn from .tdn diffs; git invokes it during diff/log/show. Hidden because it only makes sense with a file argument from git."
  },
  "arguments": {},
  "variants": [
    {
      "id": "d9ebda8f-d1a0-4f26-bb92-8836d76b7897",
      "runnableId": "python:packaging/upload.py",
      "name": "packaging/upload.py --dry",
      "args": "--dry",
      "comment": "Prints the upload plan and uploads nothing. Run this before the real sync to see which objects are new."
    },
    {
      "id": "upload-force",
      "runnableId": "python:packaging/upload.py",
      "name": "packaging/upload.py --force",
      "args": "--force",
      "comment": "Re-uploads objects that already exist in the bucket. Only for repairing a truncated or corrupted upload -- normal publishes never need it, since pinned objects are immutable."
    }
  ],
  "loadEnv": true
}
