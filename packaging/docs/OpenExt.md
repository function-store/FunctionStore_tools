---
package: OpenExt
summary: Pressing Ctrl(Cmd)+E when a COMP with a Promoted extension is selected will open the extension code in the (external) editor.
features:
  - name: OpenExt
    anchor: openext
---

## OpenExt

Pressing `Ctrl(Cmd)+E` when a COMP with a Promoted extension is selected will open the extension code in the (external) editor.
